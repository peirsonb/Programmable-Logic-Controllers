\begin{circuitikz}[
    american,
    voltage dir=RP,
    >=latex,
    %>={Latex[length=5mm, width=2mm]},
    alt={A count down timer block labeled CTD with the tag name My Counter. The preset value PRE is set to 10, and the accumulated value ACC is set to 12. Output lines extend from the right side for enable EN and done DN status.}
    ]

    \pic (C1) at (0,0) {TON};

    \node[text=BakerBlack, anchor=south] at (C1-TOP) {{\small CTD}};
    \node[text=BakerBlack, anchor=north] at (C1-TOP) {{\small My\_Counter}};
    \node[text=BakerBlack, anchor=west] at (C1-PRE) {{\small 10}};
    \node[text=BakerBlack, anchor=west] at (C1-ACC) {{\small 12}};

    
\end{circuitikz}