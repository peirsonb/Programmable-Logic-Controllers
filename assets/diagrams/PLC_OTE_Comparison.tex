\begin{circuitikz}[
    american,
    voltage dir=RP,
    >=latex,
    %>={Latex[length=5mm, width=2mm]},
    alt={Diagram showing four ladder logic sub-rungs arranged in two columns and two rows, illustrating combinations of contact types and power flow. The left column shows Examine If Closed (XIC) contacts with output coils: the top row shows an unhighlighted, false XIC contact leading to an unhighlighted output coil, while the bottom row shows an XIC contact with green power flow highlighting continuing through to the output coil. The right column shows Examine If Open (XIO) contacts with output coils: the top row shows an unhighlighted, false XIO contact leading to an unhighlighted output coil, while the bottom row shows an XIO contact with green power flow highlighting continuing through to the output coil.}
    ]

    %==========================================================================
    % Column 1 Row 1 - False XIC, false OTE
    %==========================================================================
    \draw[
        BakerBlack
    ]
    (0,0)
    coordinate(start)
    to[C, color=BakerBlack] ++(2,0)
    to[esource, color=BakerBlack] ++(2,0)
    coordinate(end);

    %==========================================================================
    % Column 1 Row 2 - True XIC, true OTE
    %==========================================================================
    \begin{scope}[yshift=-2cm]
        \draw[
            BakerBlack
        ]
        (0,0)
        coordinate(start)
        to[C, color=BakerBlack] ++(2,0)
        to[esource, color=BakerBlack] ++(2,0)
        coordinate(end);

        \begin{pgfonlayer}{bg}
            \draw[
                UpNorthGreen,
                line width=10pt,
                opacity=0.5
            ]
            (start)
            -- (end);
        \end{pgfonlayer}
    \end{scope}

    %==========================================================================
    % Column 2 Row 1 - False XIO, false OTE
    %==========================================================================
    \begin{scope}[xshift=7cm]
        \draw[
            BakerBlack
        ]
        (0,0)
        coordinate(start)
        to[vC, color=BakerBlack] ++(2,0)
        to[esource, color=BakerBlack] ++(2,0)
        coordinate(end);

        %==========================================================================
        % Column 2 Row 2 - True XIO, true OTE
        %==========================================================================
        \begin{scope}[yshift=-2cm]
            \draw[
                BakerBlack
            ]
            (0,0)
            coordinate(start)
            to[vC, color=BakerBlack] ++(2,0)
            to[esource, color=BakerBlack] ++(2,0)
            coordinate(end);

            \begin{pgfonlayer}{bg}
                \draw[
                    UpNorthGreen,
                    line width=10pt,
                    opacity=0.5
                ]
                (start)
                -- (end);
            \end{pgfonlayer}
        \end{scope}
    \end{scope}

\end{circuitikz}