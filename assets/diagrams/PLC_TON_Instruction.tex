\begin{circuitikz}[
    american,
    voltage dir=RP,
    >=latex,
    %>={Latex[length=5mm, width=2mm]},
    alt={Diagram showing a standalone TikZ Timer On Delay (TON) instruction block labeled My_Timer. The instruction block displays a Preset (PRE) value of 2000 and an Accumulated (ACC) value of 0, accompanied by connection points for its standard timer status indicators.}
    ]

    \pic (T1) at (0,0) {TON};

    \node[text=BakerBlack, anchor=south] at (T1-TOP) {{\small TON}};
    \node[text=BakerBlack, anchor=north] at (T1-TOP) {{\small My\_Timer}};
    \node[text=BakerBlack, anchor=west] at (T1-PRE) {{\small 2000}};
    \node[text=BakerBlack, anchor=west] at (T1-ACC) {{\small 0}};

    % \draw[
    %     BakerBlack
    % ]
    % (0,0)
    % -- ++(0.5,0)
    % node[
    %     draw=BakerBlack,
    %     text=BakerBlack,
    %     anchor=157.5,
    %     align=left,
    %     minimum width=2cm,
    %     minimum height=2cm
    % ](ton)
    % {{\small \textbf{Timer:} My\_Timer}\\
    % {\small \textbf{PRE:} 500}\\
    % {\small \textbf{ACC:} 0}};

    % \draw[
    %     BakerBlack
    % ]
    % (ton.22.5)
    % -- ++(0.5,0)
    % coordinate(ton_out);

    % \draw[
    %     BakerBlack
    % ]
    % (ton.east)
    % -- ++(1,0)
    % node[
    %     text=BakerBlack,
    %     anchor=north east
    % ]
    % {{\small EN}};

    % \draw[
    %     BakerBlack
    % ]
    % (ton.337.5)
    % -- ++(1,0)
    % node[
    %     text=BakerBlack,
    %     anchor=north east
    % ]
    % {{\small DN}};

    % \node[
    %     text=BakerBlack,
    %     anchor=south
    % ]
    % at (ton.north)
    % {{\small TON}};
    
\end{circuitikz}