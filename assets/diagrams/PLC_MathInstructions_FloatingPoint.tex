\begin{circuitikz}[
    american,
    voltage dir=RP,
    >=latex,
    %>={Latex[length=5mm, width=2mm]},
    alt={Diagram showing two Division instruction blocks labeled DIV, arranged side by side. The first block demonstrates integer division where Source A is set to tag Number1 with a value of 10, Source B is set to tag Number2 with a value of 3, and Destination is set to tag Result with an integer result value of 3. The second block demonstrates floating-point division where Source A is set to tag Number1 with a value of 10, Source B is set to tag Number2 with a value of 3, and Destination is set to tag Result with a floating-point result value of 3.333.}
    ]

    %=================================================================
    % DIV - result is a DINT
    %=================================================================
    \begin{scope}[
        draw=BakerBlack,
        text=BakerBlack,
        font=\small
    ]
        \pic (DIV) at (0,0) {SrcDest};
        \node[anchor=south] at (DIV-TOP) {DIV};
        \node[anchor=west] at (DIV-SRCA) {Number1};
        \node[anchor=west] at (DIV-AVAL) {10};
        \node[anchor=west] at (DIV-SRCB) {Number2};
        \node[anchor=west] at (DIV-BVAL) {3};
        \node[anchor=west] at (DIV-DEST) {Result};
        \node[anchor=west] at (DIV-DVAL) {3};
    
    \end{scope}

    %=================================================================
    % DIV - result is a REAL
    %=================================================================
    \begin{scope}[
        xshift=4.5cm,
        draw=BakerBlack,
        text=BakerBlack,
        font=\small
    ]
        \pic (DIV) at (0,0) {SrcDest};
        \node[anchor=south] at (DIV-TOP) {DIV};
        \node[anchor=west] at (DIV-SRCA) {Number1};
        \node[anchor=west] at (DIV-AVAL) {10};
        \node[anchor=west] at (DIV-SRCB) {Number2};
        \node[anchor=west] at (DIV-BVAL) {3};
        \node[anchor=west] at (DIV-DEST) {Result};
        \node[anchor=west] at (DIV-DVAL) {3.333};
    
    \end{scope}
    
\end{circuitikz}