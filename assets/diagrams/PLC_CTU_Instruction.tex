\begin{circuitikz}[
    american,
    voltage dir=RP,
    >=latex,
    %>={Latex[length=5mm, width=2mm]},
    alt={A standalone count up counter block labeled CTU with the tag name My_Counter. Inside the block, the preset value PRE is set to 5 and the accumulated value ACC is set to 0. Output status lines extend from the right side of the block for enable EN and done DN.}
    ]

    \pic (C1) at (0,0) {TON};

    \node[text=BakerBlack, anchor=south] at (C1-TOP) {{\small CTU}};
    \node[text=BakerBlack, anchor=north] at (C1-TOP) {{\small My\_Counter}};
    \node[text=BakerBlack, anchor=west] at (C1-PRE) {{\small 5}};
    \node[text=BakerBlack, anchor=west] at (C1-ACC) {{\small 0}};

    
\end{circuitikz}